{
  "private": true,
  "name": "arifure",
  "version": "1.0.0",
  "description": "A Cloudflare Workers project for static site hosting.",
  "scripts": {
    "start": "npx wrangler dev",
    "deploy": "npx wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250529.0",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.3.0"
  }
}
